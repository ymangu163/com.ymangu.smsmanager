<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ymangu.smsmanager"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.ymangu.smsmanager.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.ymangu.smsmanager.view.ConversationUI" >
            <!--指定我们要激活的是哪个  SearchableActivity -->
            <!-- 把下面的放在 <application> 中就可全局搜索 -->
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".view.SearchableActivity" />            
        </activity>
        <activity android:name="com.ymangu.smsmanager.view.FolderUI" ></activity>
        <activity android:name="com.ymangu.smsmanager.view.GroupUI" ></activity>
        <activity android:name="com.ymangu.smsmanager.view.ConversationDetailUI" ></activity>
        <activity android:name=".view.NewMessageUI"></activity>
        <activity android:name=".view.FolderDetailUI"></activity>
        <activity android:name=".view.SmsDetailUI"></activity>
        <!-- 使用系统的框架搜索 -->
        <activity android:name=".view.SearchableActivity">
           <intent-filter>
               <action android:name="android.intent.action.SEARCH" />
           </intent-filter>
           <meta-data android:name="android.app.searchable"
                   android:resource="@xml/searchable"/>
        </activity>
        
        <receiver android:name=".receiver.ReceiveSmsBroadcastReceive">
            <intent-filter>
                <action android:name="com.ymangu.smsmanager.receiver.ReceiveSmsBroadcastReceive"/>
            </intent-filter>
        </receiver>
        <provider android:name=".provider.GroupContentProvider"
            android:authorities="com.ymangu.smsmanager.provider.GroupContentProvider"></provider>
         <provider android:name=".provider.SearchableProvider"
            android:authorities="com.ymangu.smsmanager.provider.SearchableProvider" ></provider>
    </application>

</manifest>